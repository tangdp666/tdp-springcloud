<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ccread.comment.mapper.CommentMapper">

    <select id="getScoreAvg" resultType="java.lang.Integer">
        SELECT AVG(score) FROM `tb_comment` WHERE novel_id = #{navelId}
    </select>
</mapper>